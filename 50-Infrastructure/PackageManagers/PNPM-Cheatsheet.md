---
created: 2026-02-16
tags:
  - cheat-sheet
  - pnpm
  - package-manager
  - nodejs
aliases:
  - PNPM Cheatsheet
  - pnpm Reference
related:
  - NPM-Cheatsheet
  - Yarn-Cheatsheet
  - NX-Cheatsheet
  - PackageManagers-MOC
---

# PNPM — Полная шпаргалка

> [!SUMMARY] Обзор
> Быстрый и эффективный пакетный менеджер для Node.js. Использует жёсткие ссылки и симлинки для экономии места на диске.

---

## 📚 Теория

### Ключевые особенности

```bash
# Установить pnpm глобально
npm install -g pnpm

# Или через corepack (Node.js 16+)
corepack enable
corepack prepare pnpm@latest --activate

# Проверить версию
pnpm --version

# Обновить pnpm
pnpm add -g pnpm
```

## Основные команды

### Установка
```bash
pnpm install                    # Установить все зависимости
pnpm add <package>              # Добавить в dependencies
pnpm add -D <package>           # Добавить в devDependencies
pnpm add -g <package>           # Установить глобально
pnpm add <package> @version     # Добавить конкретную версию
pnpm add --exact <package>      # Зафиксировать точную версию
pnpm install --frozen-lockfile  # CI: ошибка если lockfile изменился
pnpm install --prefer-offline   # Использовать кэш при возможности
```

### Управление зависимостями
```bash
pnpm remove <package>           # Удалить пакет
pnpm update                     # Обновить все пакеты
pnpm update <package>           # Обновить конкретный пакет
pnpm update -i                  # Интерактивное обновление
pnpm outdated                   # Проверить устаревшие пакеты
pnpm list                       # Показать установленные пакеты
pnpm list --depth=0             # Только верхний уровень
pnpm why <package>              # Почему пакет установлен?
```

### Запуск скриптов
```bash
pnpm run <script>               # Запустить скрипт из package.json
pnpm <script>                   # Короткая форма
pnpm start                      # Запустить скрипт 'start'
pnpm test                       # Запустить скрипт 'test'
pnpm build -- --flag            # Передать аргументы
```

### Кэш и очистка
```bash
pnpm cache clean                # Очистить кэш
pnpm cache dir                  # Показать расположение кэша
pnpm store path                 # Показать путь к хранилищу
pnpm store prune                # Удалить неиспользуемые пакеты
```

## Как PNPM экономит место

```
node_modules/
├── .pnpm/              # Все пакеты хранятся здесь (жёсткие ссылки)
│   ├── express@4.18.0/
│   ├── react@18.2.0/
│   └── ...
├── express -> .pnpm/express@4.18.0/node_modules/express
└── react -> .pnpm/react@18.2.0/node_modules/react
```

- **Глобальное хранилище**: Пакеты хранятся один раз в `~/.pnpm-store`
- **Жёсткие ссылки**: Несколько проектов ссылаются на одни файлы
- **Симлинки**: node_modules содержит симлинки на хранилище

## PNPM Workspaces (Монорепозиторий)

### Настройка (pnpm-workspace.yaml)
```yaml
packages:
  - 'packages/*'
  - 'apps/*'
  - 'components/*'
```

### Команды workspace
```bash
pnpm add <pkg> -w                    # Добавить в корневой workspace
pnpm add <pkg> --filter <project>    # Добавить в конкретный проект
pnpm --filter <project> run build    # Запустить в конкретном проекте
pnpm -r run build                    # Запустить во всех проектах
pnpm -r --parallel run build         # Запустить параллельно
pnpm list -r                         # Показать во всех проектах
```

### Фильтры
```bash
pnpm --filter "package-name" build           # Конкретный пакет
pnpm --filter "...package-name" build        # Пакет + зависимости
pnpm --filter "package-name..." build        # Пакет + зависимые
pnpm --filter "./packages/*" build           # По шаблону пути
pnpm --filter "!./packages/excluded" build   # Исключить шаблон
```

### Пример
```bash
# Добавить зависимость в конкретный workspace
pnpm add lodash --filter @my-org/ui

# Запустить сборку в пакете и его зависимостях
pnpm --filter "...@my-org/api" build

# Запустить тесты во всех пакетах
pnpm -r run test

# Запустить сборку параллельно (быстрее)
pnpm -r --parallel --aggregate-output run build
```

## Конфигурация (.npmrc или .pnpmrc)

```ini
# .npmrc (уровень проекта)
registry=https://registry.npmjs.org/
strict-peer-dependencies=false
shamefully-hoist=true          # Поднять все deps (как npm/yarn)
auto-install-peers=true        # Авто-установка peer-зависимостей
dedupe-peer-dependents=true    # Дедупликация peers
node-linker=hoisted            # Использовать hoisted node_modules
# node-linker=isolated         # По умолчанию: isolated режим

# Расположение хранилища
store-dir=~/.pnpm-store

# Версии бок о бок
side-by-side-special-deps=true
```

## Полезные команды

```bash
# Информация о пакете
pnpm info <package>              # Показать информацию о пакете
pnpm search <query>              # Поиск пакетов

# Бинарники
pnpm bin                         # Показать папку bin
pnpm exec <command>              # Выполнить в окружении

# Линковка
pnpm link <package>              # Линковать локальный пакет
pnpm link --global               # Линковать глобально

# Аудит
pnpm audit                       # Проверка безопасности
pnpm audit fix                   # Исправить уязвимости

# Публикация
pnpm publish                     # Опубликовать пакет
pnpm login                       # Войти в реестр
pnpm whoami                      # Показать текущего пользователя
```

## CI/CD Интеграция

### GitHub Actions
```yaml
- uses: pnpm/action-setup@v2
  with:
    version: 8

- uses: actions/setup-node@v3
  with:
    node-version: 18
    cache: 'pnpm'

- run: pnpm install --frozen-lockfile
- run: pnpm -r --parallel run build
- run: pnpm -r run test
```

### Docker
```dockerfile
FROM node:18-alpine

RUN corepack enable
RUN corepack prepare pnpm@latest --activate

WORKDIR /app
COPY pnpm-lock.yaml ./
RUN pnpm fetch --frozen-lockfile

COPY . .
RUN pnpm install --frozen-lockfile --prefer-offline

CMD ["pnpm", "start"]
```

## Миграция с npm/yarn

```bash
# Удалить старые lock-файлы
rm package-lock.json yarn.lock

# Установить через pnpm
pnpm install

# Обновить CI/CD для использования pnpm
# Обновить документацию
```

## Частые проблемы

```bash
# Очистить и переустановить
rm -rf node_modules pnpm-lock.yaml
pnpm store prune
pnpm install

# Исправить проблемы с peer-зависимостями
pnpm install --no-strict-peer-dependencies

# Проверить хранилище
pnpm store status

# Проверить установку
pnpm doctor
```

## Сравнение

| Особенность | npm | yarn | pnpm |
|-------------|-----|------|------|
| Скорость установки | Средняя | Быстрая | Быстрее всех |
| Место на диске | Много | Средне | Минимум |
| node_modules | Плоский | Плоский | Вложенный |
| Фантомные зависимости | Возможны | Возможны | Запрещены |
| Workspaces | Базовые | Хорошие | Отличные |
| Поддержка монорепозиториев | Ограниченная | Хорошая | Лучшая |

---

## 🔗 Связанные заметки

- [[NPM-Cheatsheet]] — стандартный менеджер
- [[Yarn-Cheatsheet]] — альтернативный менеджер
- [[NX-Cheatsheet]] — монорепозиторий
- [[PackageManagers-MOC]] — обзор всех менеджеров
