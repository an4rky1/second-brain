---
created: 2026-02-16
tags:
  - moc
  - linux
related:
  - MOC-Infrastructure
---

# 🐧 MOC — Linux

> [!ABSTRACT] Обзор
> Linux internals, команды, системное администрирование.

---

## 🗂️ Навигация

| Тема | Файл | Статус |
|------|------|--------|
| Bash | [[Bash-Cheatsheet]] | 🟢 Готов |
| Commands | [[Linux-Commands]] | 🟢 Готов |

---

## 📁 Структура файловой системы

```
/
├── bin/          # Бинарники пользователей
├── boot/         # Загрузчик
├── dev/          # Устройства
├── etc/          # Конфиги
├── home/         # Домашние директории
├── lib/          # Библиотеки
├── opt/          # Дополнительное ПО
├── proc/         # Информация о процессах
├── root/         # Дом root
├── run/          # Runtime данные
├── sbin/         # Системные бинарники
├── tmp/          # Временные файлы
├── usr/          # Пользовательские программы
└── var/          # Логи, переменные данные
```

---

## 🔧 Essential Commands

### Файлы и директории
```bash
ls -la           # Список файлов
cd /path         # Смена директории
pwd              # Текущий путь
cp -r src dst    # Копирование
mv src dst       # Перемещение
rm -rf dir       # Удаление
```

### Процессы
```bash
ps aux           # Список процессов
top / htop       # Мониторинг
kill -9 PID      # Убить процесс
systemctl status # Статус сервиса
```

### Сеть
```bash
ip addr          # IP адреса
ss -tulpn        # Порты
curl -v url      # HTTP запрос
dig domain       # DNS lookup
```

---

## 📊 Performance Monitoring

| Команда | Назначение |
|---------|------------|
| `top` / `htop` | Процессы, CPU, RAM |
| `df -h` | Дисковое пространство |
| `du -sh *` | Размер директорий |
| `iostat` | Disk I/O |
| `vmstat` | Виртуальная память |
| `netstat` / `ss` | Сетевые соединения |
| `dmesg` | Сообщения ядра |

---

## 🛡️ Security Basics

```bash
# Права доступа
chmod 755 file      # rwxr-xr-x
chown user:group file

# Пользователи
useradd username
passwd username
sudo -i

# Firewall (ufw)
ufw enable
ufw allow 22/tcp
ufw status
```

---

## 🔗 Связанные заметки

- [[MOC-Infrastructure]] — Инфраструктура
- [[Bash-Cheatsheet]] — Bash
- [[Docker-Cheatsheet]] — Docker
- [[Security-Hardening]] — Безопасность
